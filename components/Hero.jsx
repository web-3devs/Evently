import { Box, Button, Container } from "@chakra-ui/react";
import React from "react";

export default function Hero() {
  return (
    <Container>
      <Button display={'flex'} alignItems={'center'} justifyContent={'center'}>Explore</Button>
    </Container>
  );
}
